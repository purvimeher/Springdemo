'use strict';

var Builder = require('./Builder-809f4fb1.js');

Promise.prototype.as = function (classToBuild, from) {
    if (from === void 0) { from = ''; }
    return this.then(function (input) {
        return new Promise(function (resolve, reject) {
            var result;
            try {
                if (from != '') {
                    if (input.hasOwnProperty(from)) {
                        input = input[from];
                    }
                    else {
                        throw new Error("Incoming object does not  contain a field called \"".concat(input, "\"."));
                    }
                }
                result = new Builder.Builder(classToBuild).fromJSON(input);
                resolve(result);
            }
            catch (err) {
                reject(err);
            }
        });
    });
};
